const productosSchema = require('./productos.schema')
const fabricantesSchema = require('./fabricantes.schema')
const componentesSchema = require('./componente.schema')

module.exports = {productosSchema, fabricantesSchema, componentesSchema}